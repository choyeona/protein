import styled from "styled-components";

export const Button = styled.button`
  /* 공용 텍스트 스타일을 정의합니다. */
`;

export const Heading = styled.h1`
  /* 공용 헤딩 스타일을 정의합니다. */
  /* 다른 스타일을 추가할 수 있습니다. */
`;
